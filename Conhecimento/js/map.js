/* const  maca = {
  value: 2,
}

const  peira = {
  value: 10,
}

function mapComThis(arr, thisArg){
  return arr.map(function(item){
    return item * this.value;
  }, thisArg);
}

const num = [1,2];

console.log(mapComThis(num,maca)) */

/* Primeiro formato de retorno */
