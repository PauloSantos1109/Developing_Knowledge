const loginDB = {
  userName: 'Paulo Santos',
  password: 'thayna@'
}
let loginUse = 'Paulo Santos';
let password = 'thayna@';

function verificarAcesso(permission){

  let = verify = false;

  if (loginDB.userName === loginUse && loginDB.password === password) {
    verify = true;
  }
  return verify
  
}

console.log(verificarAcesso());
